[sp]
name = "sp"
desc = "Stack Pointer"
more = "The top of the stack. The VM executes instructions in the iseq and instructions will push and pop values on the stack. The VM updates the 'sp' on the 'cfp' to point at the top of the stack"
